# groundSpring Tower Atomic Bootstrap Pipeline
# Deploys BearDog + Songbird locally on Eastgate for encrypted IPC
# between all groundSpring primals. This is the foundation that
# Node (Tower + ToadStool) and Nest (Tower + NestGate) build on.
#
# STATUS: V120 (March 23, 2026) — barraCuda v0.3.5, measurement domain niche
# Atomic: Tower (BearDog + Songbird)
# Target: Eastgate (local, single machine)
# Precision: f64-capable device selection with DF64 fallback
#
# Run: biomeos nucleus start --mode tower --node-id eastgate
#
# After Tower is running, groundSpring's biomeos.rs client discovers
# the Neural API socket and routes compute/storage through it.
# For full NUCLEUS (Tower + Node + Nest), use groundspring_nucleus_local.toml.

[graph]
id = "groundspring_tower_bootstrap"
description = "Tower atomic: BearDog + Songbird for encrypted IPC foundation"
coordination = "Sequential"

[graph.metadata]
toadstool_pin = "S158+"
toadstool_commit = "bfe7977b"
groundspring_version = "V120"
target_node = "eastgate"
domain = "measurement"

# ═══════════════════════════════════════════════════════════════════════════════
# PHASE 1: BEARDOG BOOTSTRAP (security + identity)
# ═══════════════════════════════════════════════════════════════════════════════

[[nodes]]
id = "beardog_start"
output = "beardog_status"

[nodes.operation]
name = "rpc_call"
target = "beardog"

[nodes.operation.params]
method = "tower.health"
params = { node_id = "eastgate", family_id = "groundspring" }

[nodes.constraints]
timeout_ms = 10000

# ═══════════════════════════════════════════════════════════════════════════════
# PHASE 2: SONGBIRD NUCLEATION (IPC socket mesh)
# ═══════════════════════════════════════════════════════════════════════════════

[[nodes]]
id = "songbird_nucleate"
depends_on = ["beardog_start"]
output = "songbird_status"

[nodes.operation]
name = "capability_call"

[nodes.operation.params]
capability = "ipc.nucleate"
params = { node_id = "eastgate", discover_by_capability = true }

[nodes.constraints]
timeout_ms = 15000

# ═══════════════════════════════════════════════════════════════════════════════
# PHASE 3: GROUNDSPRING REGISTRATION (register capabilities)
# ═══════════════════════════════════════════════════════════════════════════════

[[nodes]]
id = "register_capabilities"
depends_on = ["songbird_nucleate"]
output = "registration_status"

[nodes.operation]
name = "capability_call"

[nodes.operation.params]
capability = "registry.register"
params = { primal = "groundspring", capabilities = [
    "measurement.noise_decomposition",
    "measurement.anderson_validation",
    "measurement.parity_check",
    "measurement.et0_propagation",
    "measurement.regime_classification",
    "measurement.uncertainty_budget",
    "measurement.spectral_features",
    "measurement.freeze_out",
    "health.liveness",
    "health.readiness",
], family_id = "groundspring", version = "V120" }

[nodes.constraints]
timeout_ms = 10000

# ═══════════════════════════════════════════════════════════════════════════════
# PHASE 4: HEALTH VALIDATION
# ═══════════════════════════════════════════════════════════════════════════════

[[nodes]]
id = "health_check"
depends_on = ["register_capabilities"]
output = "tower_healthy"

[nodes.operation]
name = "capability_call"

[nodes.operation.params]
capability = "health.check"
params = { node_id = "eastgate", expected_primals = ["beardog", "songbird", "groundspring"] }

[nodes.constraints]
timeout_ms = 10000

# ═══════════════════════════════════════════════════════════════════════════════
# PHASE 5: PROVENANCE TRIO — record Tower bootstrap for audit
# ═══════════════════════════════════════════════════════════════════════════════

[[nodes]]
id = "provenance_session_start"
depends_on = ["health_check"]
output = "provenance_session_id"

[nodes.operation]
name = "capability_call"

[nodes.operation.params]
capability = "provenance.session_create"
params = { agent = "groundspring", experiment_id = "tower_bootstrap", version = "V120", family_id = "groundspring" }

[nodes.constraints]
timeout_ms = 10000
fallback = "skip"

[[nodes]]
id = "store_bootstrap_record"
depends_on = ["provenance_session_start"]
output = "bootstrap_stored"

[nodes.operation]
name = "capability_call"

[nodes.operation.params]
capability = "storage.put"
params = { key = "groundspring:tower:bootstrap:${TIMESTAMP}", value = "${BOOTSTRAP_SUMMARY_JSON}", family_id = "groundspring" }

[nodes.constraints]
timeout_ms = 10000
fallback = "skip"

[[nodes]]
id = "provenance_commit"
depends_on = ["store_bootstrap_record"]
output = "provenance_committed"

[nodes.operation]
name = "capability_call"

[nodes.operation.params]
capability = "provenance.session_dehydrate"
params = { session_id = "${PROVENANCE_SESSION_ID}", summary = "${BOOTSTRAP_SUMMARY_JSON}", agent = "groundspring", family_id = "groundspring" }

[nodes.constraints]
timeout_ms = 10000
fallback = "skip"
